/**
 * Shared test helpers for the Client Sign-Off Dashboard.
 *
 * Re-exports the generic in-memory repository fake foundation so test files can
 * import from a single, stable entry point: `import { InMemoryStore } from "@/test"`.
 */
export * from "./fakes";
