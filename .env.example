# Supabase configuration (wired up in task 1.2)
# Public values are safe to expose to the browser.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only service-role key. NEVER expose this to the browser.
# Used exclusively by the share-link read/write path.
SUPABASE_SERVICE_ROLE_KEY=

